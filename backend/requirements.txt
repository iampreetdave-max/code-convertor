# Runtime deps — pinned to the versions we tested against (332 passing tests).
fastapi==0.137.1
uvicorn==0.49.0
pydantic==2.12.5
python-multipart==0.0.32        # REQUIRED for file uploads (/decompile); app won't start without it
httpx==0.28.1
groq==1.0.0
requests==2.32.5
tree-sitter-language-pack==1.12.5   # multi-language output syntax validation

# Test-only (harmless in prod; keeps `pip install -r` enough to run the suite in CI).
pytest==7.4.3
pytest-asyncio==0.21.1
